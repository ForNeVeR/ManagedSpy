This solution contains 2 projects:

ManagedSpyLib:
	This is a managed VC++ project that exposes the ControlProxy class.  This class allows you to access Controls in other processes.

ManagedSpy:
	This is a C# windows forms application that is similar to spy++.  It uses ManagedSpyLib to display extra information on managed windows.
